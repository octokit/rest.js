range(1, 10)
	.each(function(item, next) {
		console.log(item);
		next();
	})
	.end(function(err) {
		console.log("end")
	});
	
list([2, 4, 1, 3]).
	filter(function(item, next) {
		next(null, item % 2 == 0);
	})
	.map(function(item, next) {
		next(null, item*2);
	})
	.map(function(item) {
		return item*2;
	})
	.toArray(function(err, arr) {
		console.log(arr);
	});
	
	

delay
splice
call
sub class generator -> use new this(...) instead of new Generator()
reduce

callEachParallel/parallel (geht nicht!)

get (xhr)
keys
values



plugins

demos
- unit test
- source code loader
- fs - stat, copy